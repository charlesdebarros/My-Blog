<h2>Posts</h2>

<% if @posts.any? %>
  <% @posts.each do |post| %>
    <%= post.title%>
    <%= post.content %>
  <% end %>
<% else %>
  <p>No posts added yet.</p>
<% end %>

<%= link_to 'Add a post', new_post_path %>